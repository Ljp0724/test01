<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/schema/mybatis-3-config.dtd">
<configuration>

    <!-- typeAliases是MyBatis中的别名处理器类，
    翻译过来就是别名的意思。别名处理器的作用就是把Java类型，
    给它起一个简单一点的别名，这样我们在XML文件中
    引入全类名的地方就可以使用别名替代。这样可以减少很多的工作量，节约开发时间-->
    <typeAliases>
        <typeAlias type="java.lang.Integer" alias="Integrt"/>
        <typeAlias type="java.lang.Long" alias="Long" />
        <typeAlias type="java.util.HashMap" alias="HashMap" />
        <typeAlias type="java.util.LinkedHashMap" alias="LinkedHashMap" />
        <typeAlias type="java.util.ArrayList" alias="ArrayList" />
        <typeAlias type="java.util.LinkedList" alias="LinkedList" />
        <typeAlias type="com.ljp.ycu.mybatis.model.UmsAdmin" alias="UmsAdmin" />
    </typeAliases>
</configuration>

                            <!-- xml配置文件-->

